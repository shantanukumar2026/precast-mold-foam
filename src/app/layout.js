import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700", "800"],
});

export const metadata = {
  title: "PRECAST | Steel Molds & EPS Foam Solutions",
  description: "Engineering precision, built for performance. Premium Steel Molds & EPS Foam Solutions for the Global Precast Concrete Industry.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en" className={inter.variable}>
      <body>{children}</body>
    </html>
  );
}

